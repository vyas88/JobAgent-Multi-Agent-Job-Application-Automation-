# Copy this file to .env and fill in real values.
# .env is gitignored and must never be committed.

# --- Postgres (Neon) ---
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require

# --- OpenAI ---
OPENAI_API_KEY=sk-your-key-here

# --- Playwright render service (run with: uvicorn src.services.render_app:app --port 8080) ---
PLAYWRIGHT_SERVICE_URL=http://localhost:8080

# --- API Authentication (FastAPI layer) ---
API_KEY=your_api_key_here

# --- Google Calendar (OAuth 2.0, least privilege calendar.events scope) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8080/oauth2callback
GOOGLE_REFRESH_TOKEN=
